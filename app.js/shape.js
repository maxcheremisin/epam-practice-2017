let Shape = (function() {
    'use strict';

    let current;
    let next;
    let size;
    let color;
    let positionX;
    let positionY;

    return {
        current: current,
        size: size,
        color: color,
        positionX: positionX,
        positionY: positionY,
        next: next
    };

})();
